services:
  portainer:
    container_name: infra_portainer
    image: portainer/portainer-ce:latest
    command: -H unix:///var/run/docker.sock
    restart: always
    ports:
      - '38000:8000'
      - '39443:9443'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /mnt/data/projects/infra/volumes/portainer:/data
    networks:
      - core-net
      - chome-net
